import SwiftUI
extension Font {
    static let cineTitle = Font.system(.largeTitle, design: .default, weight: .black)
    static let cineHeading = Font.system(.title2, design: .default, weight: .bold)
    static let cineLabel = Font.system(.caption, design: .monospaced, weight: .medium)
}
